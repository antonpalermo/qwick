{
  "name": "qwick",
  "version": "1.0.0",
  "scripts": {
    "dev": "turbo dev",
    "clean:workspace": "find -name \"node_modules\" -exec rm -rf {} +",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.15.2",
  "dependencies": {
    "dotenv-cli": "^8.0.0"
  }
}
